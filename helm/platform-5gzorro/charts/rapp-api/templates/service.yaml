apiVersion: v1
kind: Service
metadata:
  name: {{ include "rapp-api.fullname" . }}
  labels:
    {{- include "rapp-api.labels" . | nindent 4 }}
    rapp-api/component: "api"
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rapp-api.selectorLabels" . | nindent 4 }}
    rapp-api/component: "api"
